"use client";

import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
export default function PurchaseInvoiceDetailPage() {
  const params = useParams(); const [data, setData] = useState<any>(null); const [mode, setMode] = useState("bank_transfer"); const [amount, setAmount] = useState(""); const [reference, setReference] = useState(""); const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [error, setError] = useState<string | null>(null); const [saving, setSaving] = useState(false);
  async function load() { const r = await fetch(`/api/app/finance/purchase-invoices/${params.id}`, { cache: "no-store" }); const j = await r.json(); if (r.ok) { setData(j); const paid = (j.payments || []).reduce((s: number, p: any) => s + Number(p.amount || 0), 0); setAmount(Math.max(0, Number(j.invoice.total_amount) - paid).toFixed(2)); } else setError(j.error || "Unable to load purchase invoice."); }
  useEffect(() => { if (params.id) void load(); }, [params.id]);
  async function pay() { setSaving(true); setError(null); const r = await fetch(`/api/app/finance/purchase-invoices/${params.id}/payment`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ payment_mode: mode, amount: Number(amount), reference_number: reference, paid_at: date }) }); const j = await r.json(); setSaving(false); if (!r.ok) { setError(typeof j.error === "string" ? j.error : "Unable to record payment."); return; } await load(); }
  if (!data) return <div className="card">{error || "Loading purchase invoice…"}</div>;
  const paid = data.payments.reduce((s: number, p: any) => s + Number(p.amount || 0), 0); const balance = Math.max(0, Number(data.invoice.total_amount) - paid);
  return <div className="max-w-4xl"><div className="flex items-center justify-between"><div><p className="text-sm uppercase tracking-wide text-ink-500">Purchase Invoice</p><h1 className="text-2xl font-bold text-ink-900">{data.invoice.title}</h1><p className="text-sm text-ink-500">{data.invoice.invoice_number}{data.invoice.vendor_invoice_number ? ` · Vendor ref ${data.invoice.vendor_invoice_number}` : ""}</p></div><span className="badge bg-ink-100 text-ink-600">{data.invoice.status}</span></div><div className="card mt-6 space-y-6"><div className="grid gap-4 md:grid-cols-3"><div><span className="label">Vendor</span><p>{data.invoice.vendor?.name}</p></div><div><span className="label">Invoice date</span><p>{data.invoice.invoice_date}</p></div><div><span className="label">Balance payable</span><p className="font-semibold">₹{balance.toFixed(2)}</p></div></div><table className="min-w-full text-left text-sm"><thead className="bg-ink-50"><tr><th className="px-3 py-2">Description</th><th className="px-3 py-2">Qty</th><th className="px-3 py-2">Rate</th><th className="px-3 py-2">Total</th></tr></thead><tbody>{data.lineItems.map((line: any) => <tr key={line.id} className="border-t border-ink-100"><td className="px-3 py-2">{line.description}</td><td className="px-3 py-2">{line.qty}</td><td className="px-3 py-2">₹{Number(line.rate).toFixed(2)}</td><td className="px-3 py-2">₹{Number(line.line_total).toFixed(2)}</td></tr>)}</tbody></table><div className="ml-auto max-w-sm rounded bg-ink-50 p-4 text-sm"><div className="flex justify-between"><span>Base</span><span>₹{Number(data.invoice.base_amount).toFixed(2)}</span></div><div className="flex justify-between"><span>Input GST</span><span>₹{Number(data.invoice.gst_amount).toFixed(2)}</span></div><div className="flex justify-between font-semibold"><span>Total</span><span>₹{Number(data.invoice.total_amount).toFixed(2)}</span></div></div>{balance > 0 && <div className="rounded border border-ink-100 bg-ink-50 p-4"><h2 className="font-semibold">Record vendor payment</h2><div className="mt-3 grid gap-3 md:grid-cols-4"><input className="input" type="number" min="0.01" max={balance} step="0.01" value={amount} onChange={(e) => setAmount(e.target.value)} /><select className="input" value={mode} onChange={(e) => setMode(e.target.value)}><option value="bank_transfer">Bank Transfer</option><option value="cash">Cash</option><option value="cheque">Cheque</option></select><input className="input" type="date" value={date} onChange={(e) => setDate(e.target.value)} /><input className="input" placeholder="Reference" value={reference} onChange={(e) => setReference(e.target.value)} /></div><button className="btn-primary mt-3" disabled={saving || !amount} onClick={() => void pay()}>{saving ? "Posting…" : "Record Payment"}</button></div>}{error && <p className="text-sm text-red-600">{error}</p>}<div><h2 className="font-semibold">Payments</h2>{data.payments.map((p: any) => <p key={p.id} className="mt-1 text-sm text-ink-600">{p.paid_at} · ₹{Number(p.amount).toFixed(2)} · {p.reference_number || p.payment_mode}</p>)}</div></div></div>;
}
